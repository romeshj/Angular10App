import { HighlightHostBindingDirective } from './highlight-host-binding.directive';

describe('HighlightHostBindingDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightHostBindingDirective();
    expect(directive).toBeTruthy();
  });
});
