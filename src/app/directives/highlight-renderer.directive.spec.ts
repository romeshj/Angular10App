import { HighlightRendererDirective } from './highlight-renderer.directive';

describe('HighlightRendererDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightRendererDirective();
    expect(directive).toBeTruthy();
  });
});
